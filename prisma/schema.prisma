generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Story {
  id          Int     @id
  title       String
  by          String
  score       Int
  descendants Int
  url         String?
}
